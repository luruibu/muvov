# 专用于构建的 Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装构建工具
RUN apk add --no-cache git

# 复制 package 文件
COPY package*.json ./

# 安装依赖
RUN npm ci

# 复制源代码
COPY . .

# 设置构建时环境变量
ARG DOMAIN
ENV DOMAIN=$DOMAIN

# 构建并配置服务器
CMD ["sh", "-c", "npm run build && if [ -n \"$DOMAIN\" ] && [ -f docker/configure-servers.js ]; then node docker/configure-servers.js \"$DOMAIN\"; echo '✅ 服务器配置已注入'; else echo '⚠️ 跳过服务器配置'; fi"]