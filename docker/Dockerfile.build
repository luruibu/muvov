# 专用于构建的 Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装构建工具
RUN apk add --no-cache git

# 复制 package 文件
COPY package*.json ./

# 安装依赖
RUN npm ci

# 复制源代码
COPY . .

# 默认命令
CMD ["npm", "run", "build"]