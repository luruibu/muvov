{
  "name": "muvov-server",
  "version": "1.0.0",
  "description": "MUVOV P2P Chat Standalone Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build-server": "npm run build && pkg server.js --targets node18-win-x64,node18-macos-x64,node18-linux-x64 --out-path server-dist",
    "build-win": "npm run build && pkg server.js --targets node18-win-x64 --out-path server-dist",
    "build-mac": "npm run build && pkg server.js --targets node18-macos-x64 --out-path server-dist",
    "build-linux": "npm run build && pkg server.js --targets node18-linux-x64 --out-path server-dist"
  },
  "pkg": {
    "assets": [
      "dist/**/*"
    ],
    "outputPath": "server-dist"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  }
}